name: wastagram_app
description: App to record and track number of wasted items

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  location: ^3.0.0
  image_picker: ^0.6.7+22
  firebase_core: ^0.7.0
  cloud_firestore: ^0.16.0+1
  firebase_storage: ^7.0.0
  intl: ^0.16.1
  firebase_analytics: "^7.0.1"
  firebase_crashlytics: "^0.4.0+1"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/